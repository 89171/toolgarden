# JSON Toolkit — Full Reference for AI Systems

> Complete technical reference for toolgarden.xyz. ToolGarden emphasizes browser-local, no-upload workflows that reduce exposure for privacy-sensitive JSON, text, image, and PDF work.

## Site Structure

Base URL: https://toolgarden.xyz
Supported locales: en (English, default), zh (Chinese Simplified)
URL pattern: /{locale}/{tool-path}
Image tools hub: /{locale}/image
PDF tools hub: /{locale}/pdf
Blog index: /{locale}/blog
Canonical examples:
- Home: https://toolgarden.xyz/en
- Tool: https://toolgarden.xyz/en/json-format
- Image hub: https://toolgarden.xyz/en/image
- PDF hub: https://toolgarden.xyz/en/pdf
- Blog article: https://toolgarden.xyz/en/blog/why-json-can-have-comments

## Blog Reference

### How I Built a Fully Browser-Local Online Toolkit (/blog/how-i-built-browser-local-online-tool-site)
- Locales: English and Chinese Simplified
- Explains why ToolGarden was created as a privacy-first, no-upload online tool site for JSON, images, PDFs, JWTs, text, and documents
- Covers the technical implementation: static Next.js pages, thin React tool pages, lib/utils pure functions, Web Workers, Canvas, Web Crypto, WASM, browser-local file processing, and local AI model workflows
- Discusses tradeoffs such as browser memory, first-run model downloads, complex Office layout limits, unsupported legacy formats, and when a desktop tool or controlled backend is more appropriate
- Points users to JSON Formatter, Image Tools, PDF Tools, and Text Tools for browser-local workflows

### How to Choose ID Photo Size and Background: 1-Inch, 2-Inch, China Passport, and U.S. Passport Guide (/blog/make-id-photo-online-size-background-guide)
- Locales: English and Chinese Simplified
- Explains how to choose ID photo sizes and background colors for China 1-inch, China 2-inch, China passport/travel document, U.S. passport 2 x 2 inch, and custom upload forms
- Includes official reference sources from the U.S. Department of State and the Chinese Embassy in the United States, with cautions about original unedited photos and synthetic/digital changes for formal documents
- Points users to ID Photo Maker, Remove Image Background, and Image Compressor for browser-local ID photo workflows

### How Does Online Image Watermark Removal Work with Local AI Inpainting? (/blog/remove-image-watermark-local-ai-inpainting)
- Locales: English and Chinese Simplified
- Explains watermark removal as browser-local inpainting: selecting a masked area, predicting plausible background pixels, blending edges, and exporting JPG, PNG, or WebP
- Covers suitable and difficult watermark cases, including simple backgrounds, gradients, faces, text, and large translucent marks
- Points users to Image Watermark Remover and Image Editor for local image repair workflows

### How to Add Text, Image, Tiled, and Diagonal Watermarks to Images (/blog/add-watermark-to-image-text-tile-diagonal)
- Locales: English and Chinese Simplified
- Explains text watermarks, image/logo watermarks, nine-grid placement, drag positioning, tiling, diagonal tiling, opacity, rotation, and export format choice
- Covers practical use cases such as author marks, brand logos, samples, review images, and anti-repost overlays
- Points users to Image Watermark and Image Editor for browser-local watermark workflows

### How One-Click Background Removal Works with a Browser-Local Model (/blog/remove-image-background-browser-local-model)
- Locales: English and Chinese Simplified
- Explains foreground segmentation, alpha mask generation, transparent PNG output, and browser-local model processing
- Covers best and difficult image types such as single subjects, simple backgrounds, hair, transparent objects, shadows, and low-contrast subject edges
- Points users to Remove Image Background and Image to PNG for transparent image workflows

### Base64 Encoding Explained and Common Pitfalls (/blog/base64-encoding-explained-common-pitfalls)
- Locales: English and Chinese Simplified
- Explains Base64 as encoding rather than encryption, size overhead, raw Base64, Data URLs, URL-safe Base64, padding, and API payload pitfalls
- Covers common mistakes such as exposing secrets, missing padding, mixing Data URLs with raw Base64, and embedding very large images
- Points users to Info Encoder / Decoder, Image to Base64, and Base64 to Image

### LRC vs SRT Subtitle Formats and How to Edit Them Online (/blog/lrc-vs-srt-subtitle-format-edit-online)
- Locales: English and Chinese Simplified
- Compares LRC line timestamps and SRT cue start/end timestamps, with examples and common use cases
- Covers online subtitle editing workflow: upload or paste subtitles, preview with local media, calibrate timing, and export
- Points users to Subtitle Maker for LRC and SRT editing

### How to Convert Excel Data to JSON Online (/blog/convert-excel-data-to-json-online)
- Locales: English and Chinese Simplified
- Explains how Excel headers become JSON field names and each row becomes an object
- Covers sheet cleanup, empty cells, duplicate headers, dates, numbers, and first-sheet behavior before conversion
- Points users to Excel to JSON and JSON Formatter for browser-local data conversion

### How Text Diff Algorithms Find Additions, Deletions, and Changes (/blog/text-diff-algorithm-add-delete-change)
- Locales: English and Chinese Simplified
- Explains diff algorithms as sequence comparison using line, word, character, and structured levels
- Covers common subsequence ideas, additions, deletions, adjacent changes, and when JSON Diff is better than raw text diff
- Points users to Text Diff and JSON Diff for browser-local comparison workflows

### What Is the Difference Between JSON, JSON5, and JSONC? (/blog/json-json5-jsonc-differences)
- Locales: English and Chinese Simplified
- Compares strict JSON, JSONC for configuration comments, and JSON5 JavaScript-like syntax such as single quotes, trailing commas, and unquoted keys
- Explains when to use each format and when to convert back to standard JSON for APIs, databases, and third-party systems
- Points users to JSON Formatter and JSON Repair / Clean

### How to Convert Word, Excel, PowerPoint, and Images to PDF for Free (/blog/convert-word-excel-ppt-image-to-pdf-free)
- Locales: English and Chinese Simplified
- Explains browser-local PDF conversion for DOCX, XLSX/XLS, PPTX, JPG, PNG, WebP, TXT, Markdown, HTML, and related file types
- Covers conversion limitations, layout review, page count checks, image orientation, text extraction, and file-size checks
- Points users to Convert to PDF and Merge Images

### How to Merge Multiple PDFs While Keeping Page Order and Checking Bookmarks (/blog/merge-multiple-pdf-files-keep-order-bookmarks)
- Locales: English and Chinese Simplified
- Explains PDF merge order, naming files by reading order, drag sorting, page count checks, and chapter-boundary review
- Clarifies that bookmarks/outlines, forms, annotations, and complex PDF structures may need separate review after export depending on merge support
- Points users to Merge PDF and Organize PDF

### How to Split a PDF by Pages, Ranges, or File Size (/blog/split-pdf-by-pages-ranges-size)
- Locales: English and Chinese Simplified
- Explains splitting every page, splitting by page ranges, and practical workflows for meeting upload size limits
- Covers range syntax such as 1-3,5,8-10 and why exact size-based splitting can be hard when page complexity varies
- Points users to Split PDF and Extract Pages

### How to Extract Selected Pages from a PDF (/blog/extract-selected-pages-from-pdf)
- Locales: English and Chinese Simplified
- Explains extracting selected pages or ranges into one new PDF, including single pages, ranges, and mixed selections
- Covers checking actual PDF page numbers, output order, page count, file size, and when an organize tool is more appropriate
- Points users to Extract Pages and Organize PDF

### Chinese vs English Text Counting: Characters, Words, and Bytes (/blog/chinese-english-word-count-character-byte)
- Locales: English and Chinese Simplified
- Explains how Chinese and English text counting differs across characters, words, UTF-8 bytes, spaces, punctuation, and emoji
- Covers writing, SEO, forms, API fields, database limits, and why byte limits differ from visible text length
- Points users to Word Count and Info Encoder / Decoder

### How to Convert JSON to a TypeScript Interface (/blog/convert-json-to-typescript-interface)
- Locales: English and Chinese Simplified
- Explains how JSON samples are inferred into TypeScript interfaces, including strings, numbers, booleans, arrays, nested objects, and null values
- Covers post-generation review points such as empty arrays, nullable fields, interface naming, and date strings
- Points users to JSON to TypeScript and JSON Formatter for browser-local type generation workflows

### How to Fix JSON Unexpected token Errors (/blog/fix-json-unexpected-token-error)
- Locales: English and Chinese Simplified
- Explains common causes of JSON parser errors, including trailing commas, comments, parsing objects twice, HTML responses, and Python-style None values
- Provides a practical debugging order for confirming the response type, cleaning non-standard syntax, and formatting fixed JSON
- Points users to JSON Repair / Clean and JSON Formatter for browser-local JSON cleanup

### What Is JSON Schema and How Do You Validate API Data? (/blog/what-is-json-schema-api-validation)
- Locales: English and Chinese Simplified
- Introduces JSON Schema as a structural contract for JSON data, including type, properties, required, items, and format
- Explains an API validation workflow: generate a first schema from a sample, refine constraints, validate real JSON, and inspect error paths
- Points users to JSON Schema Generator and JSON Schema Validator for browser-local validation workflows

### What Is a JWT and How Do You Safely Inspect Header and Payload? (/blog/what-is-jwt-header-payload)
- Locales: English and Chinese Simplified
- Explains the three JWT segments: Header, Payload, and Signature, and clarifies that Header and Payload are Base64URL encoded rather than encrypted
- Covers safety rules for inspecting tokens, including not pasting production tokens into untrusted tools and not exposing HS256 secrets
- Points users to JWT Parser and Info Encoder / Decoder for browser-local token and encoding inspection

### How to Generate QR Codes for URLs, Wi-Fi, and Contact Cards (/blog/how-to-generate-qr-code-url-wifi-contact)
- Locales: English and Chinese Simplified
- Explains QR payload formats for URLs, Wi-Fi configuration text, and vCard contact cards
- Covers output size, quiet zone, contrast, print testing, and logo/error-correction considerations
- Points users to QR Code Generator and QR Code Decoder for browser-local QR workflows

### Why Can a QR Code Fail to Decode and How Can You Improve Scan Rate? (/blog/why-qr-code-cannot-be-decoded)
- Locales: English and Chinese Simplified
- Explains scan failures caused by blur, low resolution, missing quiet zone, weak contrast, over-compression, and dense payloads
- Recommends checking the original image with a decoder, using pixel-perfect upscaling, preserving the quiet zone, and shortening long payloads
- Points users to QR Code Decoder, Image Upscale, and Image Crop for browser-local diagnosis and repair workflows

### How to Use a Text Diff Tool to Find Differences Quickly (/blog/how-to-use-text-diff-tool)
- Locales: English and Chinese Simplified
- Explains text diff for comparing configs, product copy, logs, translations, generated output, and other plain-text content
- Compares line-level diff, word-level diff, and structured JSON diff use cases
- Points users to Text Diff and JSON Diff for browser-local comparison workflows

### Word Count, Character Count, and Byte Count: What Is the Difference? (/blog/word-count-character-byte-difference)
- Locales: English and Chinese Simplified
- Explains word count, character count, byte count, line count, and paragraph count for writing, forms, APIs, and storage limits
- Covers UTF-8 byte differences across English letters, Chinese characters, and emoji
- Points users to Word Count and Info Encoder / Decoder for browser-local text measurement and encoding workflows

### Why Do Images Look Blurry After Compression? (/blog/why-image-compression-looks-blurry)
- Locales: English and Chinese Simplified
- Explains why compressed JPG, PNG, and WebP images can become blurry, including aggressive quality settings, repeated compression, resizing, and mismatched formats
- Recommends preserving clarity by starting with moderate quality settings, choosing the right format, resizing before over-compressing, and comparing previews
- Points users to Image Compressor and Image Resize for browser-local optimization workflows

### How to Compress an Image to a Target File Size (/blog/compress-image-to-target-size)
- Locales: English and Chinese Simplified
- Explains how to reach targets such as 200KB, 500KB, or 1MB by balancing pixel dimensions, output format, quality, and image complexity
- Covers common use cases such as avatars, document photos, blog images, product images, and screenshots
- Points users to Image Compressor and Image to WebP for browser-local compression and format conversion

### Why Does a Favicon Look Blurry? (/blog/why-favicon-looks-blurry)
- Locales: English and Chinese Simplified
- Explains why favicons become blurry, including small source images, missing icon sizes, complex marks, transparency edges, and repeated compression
- Covers multi-size ICO generation, high-quality Canvas resizing, alpha preservation, favicon HTML references, and real browser preview checks
- Points users to Image to Icon for browser-local favicon generation

### How to Convert an Image to ICO (/blog/convert-image-to-ico)
- Locales: English and Chinese Simplified
- Explains how PNG, JPG, WebP, SVG, and other browser-decodable images can be turned into true ICO container files
- Covers browser-local image decoding, multi-size Canvas rendering, ICONDIR and ICONDIRENTRY structure, BGRA DIB entries, PNG-backed 256px entries, and favicon usage
- Points users to Image to Icon and Remove Image Background for transparent icon workflows

### ICO vs ICNS vs PNG Icons (/blog/ico-vs-icns-vs-png-icons)
- Locales: English and Chinese Simplified
- Compares ICO, ICNS, and PNG for website favicons, Windows icons, macOS app icons, PWA icons, and brand asset packages
- Explains ICO little-endian directory entries, ICNS resource chunks, PNG single-image structure, and multi-size PNG ZIP workflows
- Points users to Image to Icon for exporting ICO, ICNS, and PNG ZIP from one source image

### JPG, PNG, WebP, and AVIF Differences (/blog/jpg-png-webp-avif-differences)
- Locales: English and Chinese Simplified
- Compares JPG, PNG, WebP, and AVIF by compression mode, transparency support, compatibility, quality tradeoffs, and best use cases
- Covers photos, screenshots, transparent logos, web images, thumbnails, hero images, and maximum-compression workflows
- Points users to Image to WebP, Image to AVIF, Image to JPG, and Image to PNG for browser-local format conversion

### How to Convert Images to WebP (/blog/convert-images-to-webp)
- Locales: English and Chinese Simplified
- Explains how JPG, PNG, GIF, SVG, AVIF, and other browser-decodable images can be converted to WebP
- Covers browser-local decoding, Canvas rendering, WebP quality settings, transparency support, output validation, preview, and ZIP batch download
- Points users to Image to WebP and Image Compressor for web image optimization workflows

### How to Convert Images to AVIF (/blog/convert-images-to-avif)
- Locales: English and Chinese Simplified
- Explains when AVIF is useful, especially for hero images, cover images, thumbnails, and highly compressed web assets
- Covers browser-local Canvas pixel extraction, AVIF encoding, quality settings, alpha preservation, output signature validation, compatibility limits, and WebP/JPG fallbacks
- Points users to Image to AVIF and Image to WebP for modern image conversion

### PNG to JPG and JPG to PNG (/blog/png-to-jpg-jpg-to-png-transparency)
- Locales: English and Chinese Simplified
- Explains what happens when converting PNG to JPG or JPG to PNG, including transparency loss, white background fill, file-size changes, lossy vs lossless compression, and quality settings
- Covers browser-local Canvas rendering, JPG quality, PNG alpha preservation, output validation, and common use cases such as platform uploads and editing workflows
- Points users to Image to JPG and Image to PNG for local format conversion

### How to Keep Images Clear After Upscaling (/blog/keep-image-clear-after-upscaling)
- Locales: English and Chinese Simplified
- Explains why upscaled images can become blurry, including limited source pixels, high scale factors, mismatched algorithms, output compression, and source artifacts
- Compares Pixel-perfect, Smooth HD, and Sharp enhance modes for QR codes, icons, screenshots, text-heavy images, photos, transparent PNG assets, and product images
- Covers output format choices, file-size growth after upscaling, browser-local Canvas processing, pica resampling, and post-upscale compression workflows
- Points users to Image Upscale, Image Compressor, and Image Resize for browser-local image workflows

### Why Can Some JSON Files Have Comments? (/blog/why-json-can-have-comments)
- Locales: English and Chinese Simplified
- Explains why standard JSON forbids comments, why TypeScript tsconfig.json and VS Code configuration files can contain comments, and how JSONC differs from JSON5
- Covers JSONC support in TypeScript, VS Code, Visual Studio, Azure configuration, selected ESLint configs, and Monaco Editor
- Points users to JSON Repair / Clean for converting JSONC-style configuration into standard JSON

## Tool Reference

### JSON Formatter (/json-format)
- Input: Any JSON or JSON5-like text (unquoted keys, single quotes supported)
- Output: Formatted JSON with 2-space indentation + interactive tree view
- Features: Format, Minify, URL Decode, Unicode Encode/Decode
- Tree view supports: expand/collapse, node copy, node delete, node download
- Auto-formats on input change (300ms debounce)

### JSON Escape (/json-escape)
- Escape: Minifies JSON, then escapes \, ", \n, \r, \t
- Unescape: Reverses the escape operation and pretty-prints

### JSON Repair / Clean (/json-repair)
- Input: JSON-like text with common issues
- Repairs: line/block comments, trailing commas, smart quotes, single-quoted strings, unquoted object keys, True/False/None values
- Output: standard formatted JSON
- Does not use eval; parsing is done through the project JSON utility layer

### JSON Diff (/json-diff)
- Algorithm: Recursive deep comparison by JSON path
- Output: Per-path diff lines with type (added/removed/changed/unchanged)
- Color coding: green=added, red=removed, yellow=changed, gray=unchanged
- Option to hide unchanged lines

### JSON ↔ YAML (/json-yaml)
- Library: yaml@^2.7
- Both directions supported from the same view

### JSON ↔ XML (/json-xml)
- Library: fast-xml-parser@^4.5
- Wraps JSON in <root> element when converting to XML
- Preserves attributes using @_ prefix convention

### JSON → CSV (/json-to-csv)
- Input: JSON array of objects
- Headers: union of all keys across all objects
- Values: properly escaped (RFC 4180)
- Output: downloadable .csv file

### JSON → Excel (/json-to-excel)
- Library: xlsx (SheetJS) @^0.18
- Converts JSON array to Sheet1 of an .xlsx workbook
- Output: downloadable .xlsx file

### Excel → JSON (/excel-to-json)
- Library: xlsx (SheetJS) @^0.18
- Reads first sheet of .xlsx or .xls file
- Supports drag-and-drop upload

### JSON → TypeScript (/json-to-ts)
- Recursively infers types from JSON values
- Generates named interfaces for nested objects
- Detects integer vs float for number types
- Handles arrays, nested objects, string/number/boolean/null

### JSON Flatten / Unflatten (/json-flatten)
- Flatten: converts nested objects and arrays to a flat object with path keys, e.g. `user.roles.0`
- Unflatten: restores nested objects and arrays from path-key objects
- Supports custom delimiters, default `.`

### Image Tools (/image)
- Lists browser-local image editing, compression, conversion, and Base64 image encode/decode tools
- Child tool URLs use the `/image/*` namespace
- Image input data is not uploaded to the server

### Image Compressor (/image/compress)
- Compresses JPG, PNG, WebP, GIF, BMP, SVG, AVIF, and common browser-decodable images locally in the browser
- Uses Worker + OffscreenCanvas when available, with a Canvas fallback on the main thread
- Preserves the original format by default; an optional WebP mode exports visually safe WebP results
- Tries conservative output candidates and keeps the smallest visually safe result based on pixel-sample comparison
- Keeps the original file when no smaller visually safe candidate is found
- Supports preview comparison, per-file download, and ZIP batch download

### Image Upscale (/image/upscale)
- Upscales JPG, PNG, WebP, GIF, BMP, SVG, AVIF, and common browser-decodable images locally in the browser
- Supports 2x, 3x, 4x, and custom width or height with aspect-ratio preserving dimension sync
- Pixel-perfect mode disables interpolation for icons, QR codes, screenshots, pixel art, and hard-edged graphics
- Smooth HD mode uses high-quality browser interpolation for photos, gradients, and natural images
- Sharp enhance mode uses pica high-quality resampling with light unsharp masking for clearer existing edges and textures without AI generation
- Exports the upscaled image as PNG, JPG, or WebP; PNG is the default lossless output
- Uses browser Canvas APIs and does not upload image data to a server

### Remove Image Background (/image/remove-bg)
- Removes image backgrounds locally in the browser with @imgly/background-removal and onnxruntime-web
- Loads WASM and ONNX model assets on first use; subsequent uses are usually cached by the browser
- Exports a transparent PNG with the detected foreground
- Shows source and output preview comparison with a checkerboard background for transparency
- Image files are not uploaded to a server

### ID Photo Maker (/image/id-photo)
- Creates standard ID photos from everyday photos or existing ID photos locally in the browser
- Includes China 1 inch, China 2 inch, China visa, 2 x 2 inch, EU / Schengen, Japan passport, and custom millimeter-based canvas sizes
- Uses browser-local AI background removal, browser face detection when available, and foreground alpha bounds as a fallback for automatic framing
- Places the head near the 50% to 70% reference range, then lets users drag and zoom the cut-out subject manually
- Supports white, blue, red, neutral gray, and custom background colors
- Exports JPG or PNG output at 300 DPI-equivalent pixel dimensions without uploading image data to a server

### Image Watermark Remover (/image/remove-watermark)
- Inpaints selected watermark areas locally in the browser with a MI-GAN 512 ONNX Pipeline high-quality model by default, with a LaMa ONNX fallback and fast pixel repair as additional options
- Users drag or resize a rectangular selection over the area to repair
- Supports edge blending to soften the repaired area
- Exports the repaired image as JPG, PNG, or WebP; JPG uses a white background for transparent pixels
- Best suited for images the user owns or has permission to modify, especially plain backgrounds, gradients, screenshots, simple textures, and moderate photo texture repair
- Uses ONNX Runtime Web and browser Canvas APIs, and does not upload image data to a server

### Image Editor (/image/edit)
- Edits JPG, PNG, WebP, GIF, BMP, SVG, AVIF, and common browser-decodable images locally in the browser
- Uses a Fabric.js canvas so uploaded images can be edited directly without a separate preview pane
- Supports select, brush, marker, rectangle, ellipse, polyline, text, mosaic, blur, eraser, undo, and redo tools
- Rectangle and ellipse tools can draw stroked or filled shapes with selectable colors
- Mosaic and blur create editable region objects; the eraser deletes annotation objects without erasing the original image
- Exports the edited image as JPG, PNG, or WebP; JPG uses a white background for transparent pixels
- Uses browser Canvas APIs and does not upload image data to a server

### Image to Icon (/image/to-icon)
- Converts PNG, SVG, JPG, WebP, BMP, AVIF, and other browser-decodable images into ICO, ICNS, or multi-size PNG ZIP icons locally in the browser
- Loads the source image through browser image decoding, validates dimensions and file size, and renders each icon size on Canvas
- Supports draggable positioning, scale adjustment, high-quality Canvas smoothing, corner radius clipping, and optional browser-local background removal before packaging
- ICO output includes multi-resolution entries from 16 to 256px; smaller entries use 32-bit BGRA DIB data and 256px entries can use PNG bytes
- ICNS output writes an `icns` container with resource chunks such as `icp4`, `icp5`, `icp6`, `ic07`, `ic08`, `ic09`, and `ic10` up to 1024px
- PNG ZIP output provides separate transparent PNG files from 16 to 1024px for web, PWA, app, and brand asset workflows
- Supports browser-style favicon previews, per-format downloads, and ZIP downloads without uploading image files

### Image to JPG (/image/to-jpg)
- Converts PNG, WebP, GIF, BMP, SVG, AVIF, and common browser-decodable images to JPG
- Uses a white background for transparent pixels because JPG has no alpha channel
- Decodes input locally, redraws it to same-size Canvas with high-quality smoothing, exports `image/jpeg` with an adjustable quality value, and validates the JPEG signature
- Supports output quality control, preview comparison, per-file download, and ZIP batch download

### Image to PNG (/image/to-png)
- Converts JPG, WebP, GIF, BMP, SVG, AVIF, and common browser-decodable images to PNG
- Preserves transparency when the browser decoder exposes alpha information
- Decodes input locally, redraws it to same-size Canvas, exports `image/png` without a quality slider, and validates the PNG signature
- Supports preview comparison, per-file download, and ZIP batch download

### Image to WebP (/image/to-webp)
- Converts JPG, PNG, GIF, BMP, SVG, AVIF, and common browser-decodable images to WebP
- Preserves alpha when the browser decoder exposes transparency and uses an adjustable quality value to balance size and visual quality
- Decodes input locally, renders SVG sources to bitmap when needed, redraws to Canvas, exports `image/webp`, and validates the RIFF/WEBP signature
- Supports output quality control, preview comparison, per-file download, and ZIP batch download

### Image to AVIF (/image/to-avif)
- Converts JPG, PNG, WebP, GIF, BMP, SVG, and common browser-decodable images to AVIF
- Uses a browser-side AVIF encoder: Canvas pixels are read as RGBA with `getImageData`, encoded to AVIF bytes with quality settings, and validated with an AVIF file signature check
- Supports alpha preservation for transparent images when the browser and encoder path support it
- Supports output quality control, preview comparison, per-file download, and ZIP batch download
- AVIF encoding depends on browser support; unsupported browsers report an output format error instead of uploading files

### Image to Base64 (/image/to-base64)
- Converts JPG, PNG, WebP, GIF, BMP, SVG, AVIF, and common browser-decodable images to Base64 Data URLs locally in the browser
- Supports file selection, drag-and-drop image upload, and Ctrl+V clipboard image paste
- Shows the full Data URL and raw Base64 string with copy actions and TXT download
- Reads MIME type, dimensions, and file size locally; image files are not uploaded to a server

### Base64 to Image (/image/base64-to-image)
- Restores `data:image` Data URLs and raw image Base64 strings into Blob-backed image previews locally in the browser
- Detects PNG, JPG, WebP, GIF, BMP, SVG, and AVIF images by MIME type or file signature when possible
- Supports pasted Base64 text, selected `.txt` or `.base64` files, drag-and-drop text files, preview, and image download
- Image content is decoded locally and is not uploaded to a server

### Image Crop (/image/crop)
- Crops JPG, PNG, WebP, GIF, BMP, SVG, AVIF, and common browser-decodable images locally in the browser
- Supports dragging the crop frame to move it and dragging edges or corners to resize it
- Exports the selected crop area as JPG, PNG, or WebP; JPG uses a white background for transparent pixels
- Uses browser Canvas APIs and does not upload image data to a server

### Image Resize (/image/resize)
- Resizes JPG, PNG, WebP, GIF, BMP, SVG, AVIF, and common browser-decodable images locally in the browser
- Keeps the original image aspect ratio by default: entering width calculates height, and entering height calculates width
- Exports the full resized image as JPG, PNG, or WebP; JPG uses a white background for transparent pixels
- Uses browser Canvas APIs and does not upload image data to a server

### PDF Tools (/pdf)
- Lists PDF conversion and page operation tools
- Child tool URLs use the `/pdf/*` namespace
- First release focuses on browser-local PDF conversion and page-level operations
- Includes PDF to Word text extraction for text-based PDFs
- Planned additions include compress, convert to/from images/text/JSON, edit, annotate, sign, watermark, encrypt/decrypt, and OCR

### Convert to PDF (/pdf/to-pdf)
- Converts JPG, PNG, WebP, TXT, Markdown, HTML, EPUB, unencrypted legacy MOBI/PalmDOC, docx, xlsx/xls, and pptx to PDF locally in the browser
- EPUB conversion reads `META-INF/container.xml`, resolves the OPF package document, follows the spine order, extracts XHTML/HTML chapters, and renders the book text into PDF pages
- MOBI conversion supports unencrypted legacy PalmDOC/MOBI text records with no compression or PalmDOC compression
- Encrypted MOBI, AZW/KF8, and complex Kindle files need a future server-side converter such as Calibre
- Word docx conversion extracts document paragraphs from Office OpenXML
- Excel conversion uses SheetJS to extract sheet rows and render tabular text
- PowerPoint pptx conversion extracts slide text from Office OpenXML
- Text, Markdown, HTML, and Office text are rendered through browser Canvas before being embedded into PDF pages so Unicode content can be preserved
- Images are embedded into a PDF page with aspect-ratio preserving scaling
- Uses pdf-lib for PDF generation and fflate for Office OpenXML and EPUB zip extraction
- Legacy doc and ppt files require a future server-side converter such as LibreOffice and are reported as unsupported in the current browser version
- Supports multiple files, per-file PDF download, preview, and ZIP batch download
- Files are not uploaded to a server

### PDF to Word (/pdf/to-word)
- Extracts text from text-based PDF files locally in the browser
- Uses pdfjs-dist for PDF parsing and text extraction
- Generates a downloadable Word DOCX file using Office OpenXML packaging
- Preserves extracted text by page and line for quick editing and copying
- Scanned or image-only PDFs need OCR before they can become editable text
- Complex layout, tables, and image reconstruction are not fully preserved in this first version
- Files are not uploaded to a server

### Merge PDF (/pdf/merge)
- Combines multiple PDF files into a single PDF locally in the browser
- Uses pdf-lib to copy pages from each source PDF into a new document
- Supports drag-and-drop reordering plus move up/down controls
- Shows source page counts and previews the merged result
- Files are not uploaded to a server

### Split PDF (/pdf/split)
- Splits one PDF into multiple documents locally in the browser
- Supports page range groups such as `1-3; 4-6; 7`
- Can also split every page into a separate PDF
- Outputs a ZIP archive containing the split PDFs
- Files are not uploaded to a server

### Extract Pages (/pdf/extract-pages)
- Extracts selected pages from one PDF locally in the browser
- Supports page selections such as `1,3,5-7`
- Outputs one new PDF containing the selected pages
- Files are not uploaded to a server

### Organize PDF (/pdf/organize)
- Reorders, duplicates, and deletes pages from one PDF locally in the browser
- Uses a draggable page list with explicit move, duplicate, and delete controls
- Outputs a new organized PDF and previews it in the browser
- Files are not uploaded to a server

### Subtitle Maker (/subtitle-maker)
- Free online LRC and SRT subtitle editor
- Supports uploading local audio or video for playback preview and timeline calibration
- Supports uploading or pasting SRT / LRC text, parsing into editable subtitle rows
- Lets users edit start time, end time, and subtitle text line by line
- Lets users set the selected subtitle row start or end time from the current media playback position
- Exports edited subtitles as SRT or LRC text files
- Subtitle and media files are processed locally in the browser and are not uploaded to a server

### JSON Schema Generator (/json-schema)
- Output: JSON Schema Draft 2020-12
- Auto-detects formats: date-time, date, email, uri
- Marks all object properties as required
- Handles nested objects and arrays

### JSON Schema Validator (/json-schema-validate)
- Input: JSON data and a JSON Schema object
- Output: valid/invalid state plus error paths and messages
- Supports common JSON Schema keywords: type, required, properties, additionalProperties, items, enum, const, oneOf, anyOf, allOf, min/max string length, regex pattern, numeric bounds, array item bounds, object property bounds, and common formats (email, date, date-time, uri)

### JSONPath Query (/json-path)
- Library: jsonpath-plus@^10.3
- Supports full JSONPath syntax including filters: [?(@.price > 36)]
- Press Enter or click Query button to execute

### JSON Stats (/json-stats)
- Metrics: raw size (bytes), minified size, compression %, max nesting depth, total key count, object count, array count, type distribution, longest string value
- Auto-analyzes on input change (400ms debounce)

### Info Encoder / Decoder (/info-codec)
- Encodes text with legacy Encode (% / %u), Unicode escape sequences, URL percent encoding, UTF16-style \x hex escapes, Base64, hex, and JavaScript string escaping
- Computes MD5 and SHA1 hashes locally in the browser
- Decodes Unicode escape sequences, URL percent encoding, UTF16-style \x hex escapes, Base64, hex, HTML entities, JWT tokens, URL parameters, cookies, gzip payloads, and escaped strings
- URL parameter parsing preserves duplicate keys as arrays
- JWT decoding Base64URL-decodes header and payload without verifying the signature
- Cookie formatting converts semicolon-delimited cookie strings into readable JSON
- Gzip decompression accepts Base64 or hex encoded gzip data
- All operations are implemented in the project utility layer and do not upload input text

### QR Code Generator (/qr-code/generate)
- Generates PNG QR codes from URLs, plain text, contact data, Wi-Fi configuration text, or other strings
- Supports QR size, quiet-zone margin, and error correction level controls
- Uses qrcode@^1.5 for QR generation and outputs a browser-downloadable PNG data URL
- Text input is not uploaded to a server
- Links to QR Code Decoder for the reverse workflow

### QR Code Decoder (/qr-code/decode)
- Decodes uploaded PNG, JPG, WebP, GIF, and BMP images locally in the browser
- Supports live camera scanning with browser permission and local frame decoding
- Uses Canvas image data and jsQR@^1.4 with inverted-code attempts enabled
- Uploaded images and camera frames are not uploaded to a server
- Links to QR Code Generator for the reverse workflow

### JWT Parser (/jwt)
- Decode: Base64URL decodes header and payload (no key needed)
- Verify: HMAC-SHA256 via browser Web Crypto API (HS256 only)
- Shows expiry status (expired/valid) if exp claim present
- Includes example token for testing

## Architecture

- Framework: Next.js 16 with App Router
- Language: TypeScript (strict mode)
- Styling: Tailwind CSS v4 with semantic design tokens
- i18n: next-intl with [locale] routing
- All tool logic: pure functions in lib/utils/ (no React dependencies)
- Tool registry: lib/tools/registry.ts drives home page and navigation automatically
- SEO metadata: localized canonical URLs, hreflang alternates, WebApplication JSON-LD, ItemList JSON-LD, FAQ JSON-LD, and BreadcrumbList JSON-LD

## Privacy

- Zero server-side processing
- Page-level analytics may be used, but tool inputs are not uploaded
- Locale preference may be stored in a cookie for language routing
- No tool input data is uploaded or stored by the server
- All file operations (Excel, CSV) use browser File API
